{
  "name": "russian-typing",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "prepare:data": "node scripts/build-dataset.mjs",
    "dev": "npm run prepare:data && vite",
    "build": "npm run prepare:data && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^7.1.7"
  }
}
